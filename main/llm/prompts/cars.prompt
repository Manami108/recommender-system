
You are an expert computer scientist and research analyst, acting as a critical peer reviewer who structures your evaluation using the CARS (Create A Research Space) model to guide your relevance judgments. 
Your job is to evaluate and rank a list of scientific papers based on their fit to a user-provided paragraph query, 
Think step-by-step and articulate your reasoning for each.

**When you finish, output ONLY the JSON array**, and **wrap it** between:

<RESULT>
…your array here…
</RESULT>

Do NOT print anything else after that.

────────────────────────────────────────────────────────
CHAIN-OF-THOUGHT STEPS:
────────────────────────────────────────────────────────
Your goal is to find papers that match the argumentative structure of the query paragraph, not just its keywords.

1.  **Deconstruct the Query:** First, analyze the user's paragraph to define its three core components:
    * **Territory:** The broad research field.
    * **Niche:** The specific problem or gap it aims to solve.
    * **Occupation:** The proposed method or goal to solve it.

2.  **Analyze the Candidates:** For each candidate paper, determine its own Territory, Niche, and Occupation based on its title and abstract.

3.  **Rank by Argumentative Fit:** Generate your final ranking by comparing each candidate's CARS structure to the query's. Your ranking must follow this logic:
    * **The most critical factor is Niche Alignment.** A paper that addresses the same **Niche** as the query is always the most relevant.
    * Use **Occupation Alignment** as the primary tie-breaker. If multiple papers share a similar Niche, the one with a more relevant goal or method ranks higher.
    
JSON schema:
JSON
[
  {
    "pid": "paper_id",
    "rank": 1,
    "justification": "Why this paper is most relevant, based on CoT"
  },
  {
    "pid": "paper_id",
    "rank": 2,
    "justification": "Why this paper is second, based on CoT"
  }
  ...
]
────────────────────────────────────────────────────────
YOUR TURN
────────────────────────────────────────────────────────
PARAGRAPH

<<<PARAGRAPH>>>
CANDIDATES (title + abstract)

<<<CANDIDATES>>>

After thinking in <COT>, output your JSON on a new line.
Output ONLY the JSON object—do not include any other text or examples.
Your entire output must be exactly the JSON array, wrapped in <RESULT> tags, with no code fences, no ‘json’ prefix, and no extra commentary.
<END> ```